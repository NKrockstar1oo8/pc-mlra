services:
  - type: web
    name: pc-mlra
    env: python
    buildCommand: pip install -r requirements_flask.txt  # or requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /api/health
    autoDeploy: true